# AgriGuard AI Frontend

React frontend for AgriGuard AI.

## Run locally

```bash
npm install
npm run dev
```

Set `VITE_API_URL` to your backend `/analyze` endpoint if it differs from `http://localhost:8000/analyze`.
